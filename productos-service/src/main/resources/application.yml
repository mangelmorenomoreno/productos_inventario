spring:
  application:
    name: productos-service
  datasource:
    url: jdbc:postgresql://localhost:5432/productos-db
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8081
